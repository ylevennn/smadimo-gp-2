## Start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### Требуемые переменные окружения

- `STEAM_API_TOKEN` - получить [тут](https://steamcommunity.com/dev/apikey)